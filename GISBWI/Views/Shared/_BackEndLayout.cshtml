<!DOCTYPE html>
<html lang="en">
     <head>
         <title>admin Sistem Informasi Geografis</title>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="">
        <meta name="keywords" content="">
        <meta name="author" content="">
       <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
        <meta name="viewport" content="width=device-width" />
        @Styles.Render("~/Content/css")
        @Scripts.Render("~/bundles/modernizr")
         <script src="~/Scripts/ckeditor/ckeditor.js" ></script>
         <script src="~/Scripts/ckeditor/adapter/jquery.js" ></script>
      
        
  <script src="~/Scripts/respond.min.js"></script>
  <!--[if lt IE 9]>
  <script src="js/html5shiv.js"></script>
  <![endif]-->

  <!-- Favicon -->
         
  <link rel="shortcut icon" href="img/favicon/favicon.png">
    </head>
    <body>
        <!-- Header starts -->
        <header>
            <div class="container">
              <div class="row">

                <!-- Logo section -->
                <div class="col-md-4">
                  <!-- Logo. -->
                  <div class="logo">
                    <h1><a href="#">Mac<span class="bold">admin</span></a></h1>
                    <p class="meta">something goes in meta area</p>
                  </div>
                  <!-- Logo ends -->
                </div>


                <!-- Data section -->

                <div class="col-md-4">
                  <div class="header-data">

                    <!-- Traffic data -->
                    <div class="hdata">
                      <div class="mcol-left">
                        <!-- Icon with red background -->
                        <i class="fa fa-signal bred"></i> 
                      </div>
                      <div class="mcol-right">
                        <!-- Number of visitors -->
                        <p><a href="#">7000</a> <em>visits</em></p>
                      </div>
                      <div class="clearfix"></div>
                    </div>

                    <!-- Members data -->
                    <div class="hdata">
                      <div class="mcol-left">
                        <!-- Icon with blue background -->
                        <i class="fa fa-user bblue"></i> 
                      </div>
                      <div class="mcol-right">
                        <!-- Number of visitors -->
                        <p><a href="#">3000</a> <em>users</em></p>
                      </div>
                      <div class="clearfix"></div>
                    </div>

                    <!-- revenue data -->
                    <div class="hdata">
                      <div class="mcol-left">
                        <!-- Icon with green background -->
                        <i class="fa fa-money bgreen"></i> 
                      </div>
                      <div class="mcol-right">
                        <!-- Number of visitors -->
                        <p><a href="#">5000</a><em>orders</em></p>
                      </div>
                      <div class="clearfix"></div>
                    </div>                        

                  </div>
                </div>

                <div class="col-md-4">
                     @Html.Partial("_LoginPartial")
                </div>
              </div>
            </div>
          </header>
        <!-- Header ends -->
        <!-- Main content starts -->
        <div class="content">

  	        <!-- Sidebar -->
            <div class="sidebar">
                <div class="sidebar-dropdown"><a href="#">Navigation</a></div>

                <!--- Sidebar navigation -->
                <!-- If the main navigation has sub navigation, then add the class "has_sub" to "li" of main navigation. -->
                <ul id="nav">
                   <li class="open"><a href="/Home"><i class="fa fa-home"></i> Dashboard</a></li>
                    <li class="has_sub">
			        <a href="#"><i class="fa fa-users"></i>Manajemen Pengguna</a>
                    <ul>
                      <li>@Html.ActionLink("Daftar User", "Index","admin")</li>
                      <li>@Html.ActionLink("Tambah User", "Create","admin")</li>
                    </ul>
                  </li>  
                  <li class="has_sub">
			        <a href="#"><i class="fa fa-file-o"></i>Manajemen SKPD</a>
                        <ul>
                      <li>@Html.ActionLink("Daftar SKPD", "Index","artikel")</li>
                      <li>@Html.ActionLink("Tambah SKPD", "Create","artikel")</li>
                    </ul>
                  </li> 
                  <li class="has_sub">
			        <a href="#"><i class="fa fa-file-o"></i>Manajemen Layer</a>
                        <ul>
                      <li>@Html.ActionLink("Daftar Peta", "Index","Default1")</li>
                      <li>@Html.ActionLink("Tambah Peta", "Create","Default1")</li>
                    </ul>
                  </li> 
                 </ul>
              </div>
            <div class=" col-lg-2 col-sm-2 col-xs-2 col-sm-2"></div>
            <div class="col-lg-10 col-xs-10 col-md-10 col-sm-10">
                <div id="body">
                    @RenderSection("featured", required: false)
                    <section class="content-wrapper main-content clear-fix">
                        @RenderBody()
                    </section>
                </div>
        
                @Scripts.Render("~/bundles/jquery")
                @RenderSection("scripts", required: false)
            </div>
        </div>
        <!-- Sidebar ends -->
        <!-- Content ends -->
        <!-- Footer ends -->
        
 <!-- Scroll to top -->
<span class="totop"><a href="#"><i class="fa fa-chevron-up"></i></a></span> 

<//body>
<!-- JS -->
<script src="~/Scripts/jquery.js"></script> <!-- jQuery -->
<script src="~/Scripts/bootstrap.min.js"></script> <!-- Bootstrap -->
<script src="~/Scripts/jquery-ui.min.js"></script> <!-- jQuery UI -->
<script src="~/Scripts/fullcalendar.min.js"></script> <!-- Full Google Calendar - Calendar -->
<script src="~/Scripts/jquery.rateit.min.js"></script> <!-- RateIt - Star rating -->
<script src="~/Scripts/jquery.prettyPhoto.js"></script> <!-- prettyPhoto -->
<script src="~/Scripts/jquery.slimscroll.min.js"></script> <!-- jQuery Slim Scroll -->
<script src="~/Scripts/jquery.dataTables.min.js"></script> <!-- Data tables -->

<!-- jQuery Flot -->
<script src="~/Scripts/excanvas.min.js"></script>
<script src="~/Scripts/jquery.flot.js"></script>
<script src="~/Scripts/jquery.flot.resize.js"></script>
<script src="~/Scripts/jquery.flot.pie.js"></script>
<script src="~/Scripts/jquery.flot.stack.js"></script>

<!-- jQuery Notification - Noty -->
<script src="~/Scripts/jquery.noty.js"></script> <!-- jQuery Notify -->
<script src="~/Scripts/themes/default.js"></script> <!-- jQuery Notify -->
<script src="~/Scripts/layouts/bottom.js"></script> <!-- jQuery Notify -->
<script src="~/Scripts/layouts/topRight.js"></script> <!-- jQuery Notify -->
<script src="~/Scripts/layouts/top.js"></script> <!-- jQuery Notify -->
<!-- jQuery Notification ends -->

<script src="~/Scripts/sparklines.js"></script> <!-- Sparklines -->
<script src="~/Scripts/jquery.cleditor.min.js"></script> <!-- CLEditor -->
<script src="~/Scripts/bootstrap-datetimepicker.min.js"></script> <!-- Date picker -->
<script src="~/Scripts/jquery.onoff.min.js"></script> <!-- Bootstrap Toggle -->
<script src="~/Scripts/filter.js"></script> <!-- Filter for support page -->
<script src="~/Scripts/custom.js"></script> <!-- Custom codes -->
<script src="~/Scripts/charts.js"></script> <!-- Charts & Graphs -->

<!-- Script for this page -->
<script type="text/javascript">

    $(function () {

        /* Bar Chart starts */

        var d1 = [];
        for (var i = 0; i <= 20; i += 1)
            d1.push([i, parseInt(Math.random() * 30)]);

        var d2 = [];
        for (var i = 0; i <= 20; i += 1)
            d2.push([i, parseInt(Math.random() * 30)]);


        var stack = 0, bars = true, lines = false, steps = false;

        function plotWithOptions() {
            $.plot($("#bar-chart"), [d1, d2], {
                series: {
                    stack: stack,
                    lines: { show: lines, fill: true, steps: steps },
                    bars: { show: bars, barWidth: 0.8 }
                },
                grid: {
                    borderWidth: 0, hoverable: true, color: "#777"
                },
                colors: ["#ff6c24", "#ff2424"],
                bars: {
                    show: true,
                    lineWidth: 0,
                    fill: true,
                    fillColor: { colors: [{ opacity: 0.9 }, { opacity: 0.8 }] }
                }
            });
        }

        plotWithOptions();

        $(".stackControls input").click(function (e) {
            e.preventDefault();
            stack = $(this).val() == "With stacking" ? true : null;
            plotWithOptions();
        });
        $(".graphControls input").click(function (e) {
            e.preventDefault();
            bars = $(this).val().indexOf("Bars") != -1;
            lines = $(this).val().indexOf("Lines") != -1;
            steps = $(this).val().indexOf("steps") != -1;
            plotWithOptions();
        });

        /* Bar chart ends */

    });


    /* Curve chart starts */

    $(function () {
        var sin = [], cos = [];
        for (var i = 0; i < 14; i += 0.5) {
            sin.push([i, Math.sin(i)]);
            cos.push([i, Math.cos(i)]);
        }

        var plot = $.plot($("#curve-chart"),
               [{ data: sin, label: "sin(x)" }, { data: cos, label: "cos(x)" }], {
                   series: {
                       lines: { show: true, fill: true },
                       points: { show: true }
                   },
                   grid: { hoverable: true, clickable: true, borderWidth: 0 },
                   yaxis: { min: -1.2, max: 1.2 },
                   colors: ["#1eafed", "#1eafed"]
               });

        function showTooltip(x, y, contents) {
            $('<div id="tooltip">' + contents + '</div>').css({
                position: 'absolute',
                display: 'none',
                top: y + 5,
                left: x + 5,
                border: '1px solid #000',
                padding: '2px 8px',
                color: '#ccc',
                'background-color': '#000',
                opacity: 0.9
            }).appendTo("body").fadeIn(200);
        }

        var previousPoint = null;
        $("#curve-chart").bind("plothover", function (event, pos, item) {
            $("#x").text(pos.x.toFixed(2));
            $("#y").text(pos.y.toFixed(2));

            if ($("#enableTooltip:checked").length > 0) {
                if (item) {
                    if (previousPoint != item.dataIndex) {
                        previousPoint = item.dataIndex;

                        $("#tooltip").remove();
                        var x = item.datapoint[0].toFixed(2),
                            y = item.datapoint[1].toFixed(2);

                        showTooltip(item.pageX, item.pageY,
                                    item.series.label + " of " + x + " = " + y);
                    }
                }
                else {
                    $("#tooltip").remove();
                    previousPoint = null;
                }
            }
        });

        $("#curve-chart").bind("plotclick", function (event, pos, item) {
            if (item) {
                $("#clickdata").text("You clicked point " + item.dataIndex + " in " + item.series.label + ".");
                plot.highlight(item.series, item.datapoint);
            }
        });

    });

    /* Curve chart ends */
    /* Ckeditor */
    $(function () {
        $('#ckeditor-area').ckeditor();
    });
</script>
</html>
